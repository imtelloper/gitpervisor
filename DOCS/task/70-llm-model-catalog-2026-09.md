# 태스크 70 — 로컬 LLM 모델 카탈로그 확장 (2026-09)

> 배경: 사용자 질문 "성능 좋으면서 무료로 쓸 수 있는 모델 더 찾아줘. gemma4 같은 거 좋지 않냐?"
> 결론부터 — **Gemma 4는 실재하고, Gemma를 카탈로그에서 뺐던 사유가 전부 사라졌다.**
> 59 §3.5 카탈로그(Qwen3 3종)를 **6종**으로 늘린다 — 후보 4종을 받아 돌려 보고 실측으로 걸렀다(§6.3, §8).
> 코드 변경은 `MODELS` 배열 + 프론트 출처 표기 + 다운로드 실패 판정 + `-np 1`(§7) + 요약 프롬프트 템플릿화(§8).

---

## 1. 판정 기준 — 넷 다 통과해야 들어온다

앱이 사용자를 대신해 모델을 **자동으로 내려받는다.** 그래서 "무료로 받을 수 있나"가 아니라
**"앱에 자동 다운로드시켜도 고지·사용제한 의무가 안 따라붙나"** 가 기준이다.

| # | 기준 | 왜 |
|---|---|---|
| 1 | **무게이트** (`gated:false`) | `resolve/main`을 토큰 없이 GET 한다. 게이트면 401 |
| 2 | **단일 `.gguf`** | `ModelSpec.file`이 하나다. 분할(`-00001-of-`)·mmproj 의존은 못 쓴다 |
| 3 | **의무 없는 라이선스** (Apache-2.0 / MIT) | Llama를 뺀 사유가 "고지 의무"였다. 같은 잣대를 그대로 적용한다 |
| 4 | **b10809이 아는 아키텍처** | 런타임이 고정 빌드다(2026-09-04). 그 뒤 아키텍처는 못 읽는다 |

크기 티어는 저사양 ≈1.8GB · 기본 ≈2.5GB · 품질 ≈5GB · 최상위 ≤7GB.

---

## 2. 가장 중요한 정정 — llama-server는 **jinja가 기본 ON**이다

조사 도중 "Gemma 4는 챗 템플릿이 b10809 레거시 내장 목록(54종)에 없으니 `--jinja` 없는 현재
인자로는 못 쓴다"는 판정이 나왔다. **그 전제가 틀렸다.** b10809 소스 실측:

- `common/common.h:638` → `bool use_jinja = true;` — **기본값이 true**
- `common/arg.cpp:1398~1404` per-example 덮어쓰기에서 `use_jinja = false`를 주는 것은
  `LLAMA_EXAMPLE_COMPLETION`·`LLAMA_EXAMPLE_MTMD` 뿐. **`LLAMA_EXAMPLE_SERVER` 분기는
  `n_parallel = -1`만 설정하고 jinja를 끄지 않는다.**
- 서버 소스 6개(`server-context.cpp`·`server-common.cpp`·`server-models.cpp`·`server-chat.cpp`·
  `server-task.cpp`·`main.cpp`)에 `use_jinja` **재대입 없음**(읽기만 6건).
- `src-tauri/src/llm/server.rs`의 인자 배열에 `--jinja`도 `--no-jinja`도 없다 → 기본값이 그대로 산다.

따라온 결론 셋:

1. **Gemma 4는 인자를 하나도 안 고치고 돈다.** `common/chat.cpp:3585`에 전용 분기
   (`common_chat_params_init_gemma4`, `COMMON_CHAT_FORMAT_PEG_GEMMA4`)가 이미 있다.
2. **`chat.rs`의 `chat_template_kwargs: {"enable_thinking": false}`는 정상 작동한다.**
   kwargs를 소비하는 코드(`chat.cpp:3676`)가 `common_chat_templates_apply_jinja` 안에 있어서
   jinja 경로일 때만 먹는데, 그 경로가 기본이다. 2차 방어도 있다 — `common.h:649`의
   `reasoning_format` 기본값이 DEEPSEEK이라 사고가 나더라도 `reasoning_content`로 갈라져
   `message.content`에는 안 섞인다.
3. **모델 후보를 레거시 내장 템플릿 목록으로 떨어뜨리면 안 된다.** 그 목록은 `--no-jinja`를
   명시할 때만 의미가 있다. 올바른 기준은 "GGUF에 jinja 템플릿이 있고 b10809 minja가 파싱하는가"이고,
   실패하면 **서버가 기동하지 않는다**(조용한 품질 저하가 아니라 관측 가능한 실패다).

> **`--no-jinja`를 추가하지 마라.** 붙이는 순간 Gemma 4가 즉사한다.

---

## 3. Gemma 4 — 제외 사유가 사라진 경위

`acquire.rs`의 옛 주석은 `제외: Gemma 3(HF 토큰 게이트)`였다. 두 군데가 틀렸다.

- **게이트는 진짜 사유가 아니었다.** 커뮤니티 미러(unsloth·bartowski·ggml-org)는 `gated:false`라
  게이트는 우회된다. 진짜 사유는 **라이선스**다 — Gemma ToU는 ①약관 사본 전달 ②AUP를 하위 계약에
  **집행 가능 조항으로 삽입** ③NOTICE 동봉을 요구한다. Llama 제외 사유와 동급 이상이다.
- **Gemma 4에는 그게 안 붙는다.** `license: apache-2.0`이고, Gemma 이용약관 페이지가 스스로
  Gemma 4를 적용범위에서 뺀다("For Gemma 4 terms, see the Gemma 4 license"; Appendix 목록에
  Gemma 1/1.1/2/3/3n만 있고 4가 없다). HF 실측도 `gated:false`
  (대조: `google/gemma-3-4b-it`은 `gated:"manual"`).

실측 값(2026-09-18 채록, 2026-09-21 재확인 — 동일):

| 레포 | 파일 | 바이트 | sha256(앞 8) |
|---|---|---|---|
| `google/gemma-4-E4B-it-qat-q4_0-gguf` | `gemma-4-E4B_q4_0-it.gguf` | 5,154,941,280 | `676c3507` |
| `google/gemma-4-12B-it-qat-q4_0-gguf` | `gemma-4-12b-it-qat-q4_0.gguf` | 6,975,879,296 | `93567e57` *(속도로 탈락 §6.3)* |

**함정 둘**:
- 파일명 어순이 사이즈마다 다르다. E4B는 `gemma-4-E4B_q4_0-it.gguf`, 12B는
  `gemma-4-12b-it-qat-q4_0.gguf`. 규칙으로 유추해 쓰면 404다.
- `mmproj-*.gguf`는 비전 프로젝터다. 안 받으면 텍스트로 정상 동작한다.

공식 GGUF가 전부 **QAT**(양자화 인식 학습)라 q4_0의 양자화 손실이 학습 단계에 흡수돼 있다 —
같은 4bit라도 일반 사후 양자화보다 유리하다. 26B-A4B(13.45GB)·31B(17.65GB)는 크기로 제외.
E2B는 최소 Q4가 2.62GB라 **저사양 티어를 못 채우고** 기본 티어도 넘어 제외.

---

## 4. 채택 — 6종 (기존 3 + 신규 3)

**후보 4종을 벤치마크로 뽑아 전부 받아 돌려 본 뒤 2종을 떨어뜨렸고**(§6.3), 그중 12B 는
프롬프트를 고친 뒤 재측정에서 1위가 돼 되돌렸다(§8).

| id | 티어 | 출처 | 크기 | 라이선스 |
|---|---|---|---|---|
| `qwen3-4b-q4` **(기본)** | 기본 (기존) | `Qwen` | 2.33GB | Apache-2.0 |
| `qwen3-4b-2507-q4` | 기본 상위 **(신규)** | `bartowski` | 2.33GB | Apache-2.0 |
| `qwen3-1.7b-q8` | 저사양 (기존) | `Qwen` | 1.71GB | Apache-2.0 |
| `qwen3-8b-q4` | 품질 (기존) | `Qwen` | 4.68GB | Apache-2.0 |
| `gemma4-e4b-qat-q4` | 품질 **(신규)** | `google` | 4.80GB | Apache-2.0 |
| `gemma4-12b-qat-q4` | 배치/예약 **(신규)** | `google` | 6.50GB | Apache-2.0 |

기존 3종은 **지우지 않는다** — 설정에 그 id가 저장돼 있고 이미 받아 둔 사용자가 있다.
지우면 `모르는 모델: <id>`로 죽는다. 같은 이유로 **기본값도 바꾸지 않았다**(§6.4).

### 4.1 신규 2종의 근거

- **Qwen3 4B Instruct 2507** — 조사에서 **유일하게 동일 출처·동일 조건 비교표로 현 항목을 이긴다**
  (공식 카드가 Qwen3-4B Non-Thinking과 직접 비교): MMLU-ProX 49.6→61.6, MultiIF 61.3→69.0,
  IFEval 81.2→83.4, WritingBench 68.5→83.4, Arena-Hard v2 9.5→43.4. 카드 원문이
  "does not generate `<think></think>` blocks ... `enable_thinking=False` is no longer required" —
  **사고 모드가 구조적으로 없다.** 크기가 현 기본과 480바이트 차이라 티어·뱃지를 그대로 쓴다.
  **A/B에서도 확인**: 97 vs 현 기본 83(§6.1).
- **Gemma 4 E4B QAT** — MMMLU 76.6(Qwen3-8B 비사고 66.9 대비 +9.7). 유효 4.5B로 8B 티어 자리를
  더 작은 파일로 채운다. **A/B 98.5로 현 8B(110) 다음**이고 특히 번역이 강하다(§6.4).

### 4.2 커뮤니티 변환본을 쓰는 항목

`qwen3-4b-2507-q4`(bartowski)는 벤더 공식 GGUF가 **없다**(`Qwen/Qwen3-4B-Instruct-2507-GGUF` 404).
재업로드 위험은 "공식 vs 커뮤니티" 축이 아니다 — 오히려 반대다:

| 레포 | 마지막 변경 | 비고 |
|---|---|---|
| `bartowski/Qwen_Qwen3-4B-Instruct-2507-GGUF` | 2025-08-06 | 13개월 무변경 |
| `google/gemma-4-E4B-it-qat-q4_0-gguf` | 2026-07-17 | **본체 재업로드**(`09f6f2a1…`→`676c3507…`) |
| `ibm-granite/granite-4.2-3b-GGUF` | 2026-09-02 | 같은 파일명이 12일 만에 교체 |

**공식 레포도 sha가 불변이 아니다.** 그래서 §5의 다운로드 판정을 고쳤다.

---

## 5. 다운로드 실패 판정 — 재업로드를 변조와 구분한다

`download_verified`에 `expect_size`를 넘겨 **본문을 받기 전에** Content-Length로 먼저 막는다.
해시가 낡는 흔한 경로가 변조가 아니라 업스트림 재업로드이기 때문이다.

- 크기 불일치 → `원본 파일이 교체됐습니다 — <url> 크기가 X인데 앱은 Y를 기대합니다.
  앱을 업데이트하면 새 해시로 받습니다.` (6.5GB를 다 받고 실패하는 대신 첫 응답에서 끝난다)
- 해시 불일치 → 받은 값·기대값·URL을 전부 메시지에 담는다(종전 `무결성 검증 실패 — 다운로드 변조
  의심`은 원인을 오도했다).

---

## 6. A/B 실측 — 앱과 같은 인자·같은 프롬프트

> 벤치마크 수치는 전부 **다국어 평균**이라 한국어 단독 우열은 웹 조사로 판정 불가였다
> (Gemma 4·Qwen3.5 카드에 Korean 언급 0회, HF `language` 필드 None). 그래서 직접 쟀다.

**하네스**: `llama-server` b10809를 앱과 같은 인자로 띄운다
(`-c 8192 -ngl 99 -t 24 --api-key … --no-webui`), 요청 본문도 `chat.rs run_chat()`과 같다
(`chat_template_kwargs:{enable_thinking:false}`). 과제 4개:

| 과제 | 출처 | 프롬프트 |
|---|---|---|
| `summary-day` | 이 저장소 2026-09-18 커밋 7건 | `report.ts buildMessages()` 원문(한국어, 형식 강제) |
| `summary-week` | 2026-09-12~18 커밋 | 같음(주간) |
| `translate-ko2en` | CLAUDE.md 한 문단(경로·식별자 포함) | `translate.ts messagesFor()` 원문 |
| `translate-en2ko` | b10809 소스 설명 영문(식별자·줄번호 포함) | 같음 |

**채점**: 출력 7개를 A~G로 섞어(케이스마다 다른 순열) **모델명을 감춘 채** 독립 심사원 2명이
축 3개 × 0~5점으로 채점했다. 키는 채점이 끝난 뒤에 풀었다.

### 6.1 블라인드 점수 (심사원 2명 합산, 케이스당 최대 30 · 총 120)

| 모델 | day | week | ko→en | en→ko | 합계 |
|---|---|---|---|---|---|
| Qwen3 8B Q4 *(현 품질)* | 25 | 28 | 30 | 27 | **110** |
| Gemma 4 12B QAT | 17 | 26 | 29.5 | 30 | 102.5 |
| **Gemma 4 E4B QAT** | 24 | 18 | 27 | 29.5 | **98.5** |
| **Qwen3 4B 2507 Q4** | 20 | 24 | 28 | 25 | **97** |
| Qwen3 4B Q4 *(현 기본)* | 22 | 12.5 | 26.5 | 22 | 83 |
| Qwen3 1.7B Q8 *(현 저사양)* | 22 | 12 | 24 | 18 | 76 |
| Mi:dm 2.0 Mini Q4 | 12 | 4 | **5** | 27 | **48** |

### 6.2 속도 (같은 입력·같은 인자, 단독 회차)

| 모델 | 로드 | day | week | 프롬프트 처리 | 생성 |
|---|---|---|---|---|---|
| Qwen3 1.7B Q8 | 3.6s | 2.6s | 12.1s | 6,100~10,000 t/s | ~170 t/s |
| Mi:dm 2.0 Mini | 3.0s | 1.2s | 3.7s | 2,900~5,900 t/s | ~154 t/s |
| Qwen3 4B Q4 | 3.5s | 3.6s | 8.0s | 3,100~4,000 t/s | ~120 t/s |
| Qwen3 4B 2507 | 3.5s | 3.1s | 12.8s | 2,600~3,900 t/s | ~91 t/s |
| Qwen3 8B Q4 | 6.0s | 4.6s | 14.1s | 1,450~2,400 t/s | ~60 t/s |
| Gemma 4 E4B QAT | 5.0s | 2.8s | 13.2s | 1,300~3,100 t/s | ~63 t/s |
| Gemma 4 12B QAT | 24.6s | 17.9s | **85.0s** | 300~590 t/s | **~13 t/s** |

> **측정 함정 하나 기록해 둔다.** 처음 잰 Gemma 4 E4B의 프롬프트 처리는 **15 t/s**였다. 그 스모크
> 테스트를 **다른 모델 다운로드가 디스크를 포화시키는 중에** 돌렸기 때문이다. 단독 회차에서
> 2,284~3,126 t/s로 재현됐다 — 150~200배 차이다. 느린 회차의 빨강은 먼저 단독 재현하라는
> e2e 교훈이 여기도 그대로 적용된다.

### 6.3 실측이 뒤집은 것 — 두 후보 탈락

**웹 조사 1·4순위가 실제 앱 프롬프트에서 떨어졌다.** 벤치마크 점수는 앱의 계약을 재지 않는다.

- **Mi:dm 2.0 Mini — 탈락(48/120, 최하위).** 한국어 벤치마크만 보면 후보 중 유일하게 Qwen3를
  수치로 이겼는데, 실제로는:
  - **한→영 번역에 한국어 원문을 그대로 돌려줬다.** 챗 템플릿이 시스템 메시지 앞에 KT 페르소나
    블록(약 1,400자, "어시스턴트는 기본적으로 '한국어'를 사용한다")을 무조건 끼워 넣어 영어 지시를
    덮는다. 61의 자동 방향은 한글 20% 이상이면 **영어로** 번역하는 것이라 주 경로가 죽는다.
    프롬프트 토큰도 같은 입력에서 673 vs 234로 부풀었다.
  - 요약에서 형식 지시문을 머리글에 그대로 베꼈다(`## 한 줄 요약 / 2026-09-18 (금)에…`,
    `## 다음 할 일 (3개 이하)`)고, **커밋 해시를 0개** 인용했다.
- **Gemma 4 12B QAT — 1차 판정은 "속도로 탈락"이었으나 §9에서 뒤집혔다.** 12GB VRAM에서
  생성 13 t/s, 주간 요약 85초(8B는 14초)라 처음엔 뺐다. 그런데 그 판정의 품질 근거는 **케이스당
  1회**였고, §9의 재측정(3회 × 심사원 3명)에서 12B가 8B를 크게 앞선다. 카탈로그에 되돌렸다.
  뱃지 한계는 그대로다 — `recommend()`가 `vram ≥ size*1.15`(7.47GiB)만 보므로 12GB 카드에도
  "GPU 전체"가 뜨는데 실제로는 느리다. 그래서 note에 초를 적었다.

### 6.4 채택 확정

- **`qwen3-4b-2507-q4` 채택** — 97 vs 현 기본 83. 주간 요약에서 특히 벌어진다(24 vs 12.5):
  현 기본은 주간에서 **커밋 해시를 하나도 인용하지 않았고**, 2507은 15개를 인용했다.
- **`gemma4-e4b-qat-q4` 채택** — 98.5로 현 8B(110) 다음. 번역이 강하다(en→ko 29.5/30, 식별자·
  줄번호를 글자 그대로 보존). 다만 주간 요약 형식은 약하다(18/30 — `## 한 줄 요약`을 빼고
  제 나름의 머리글을 만든다). 크기는 8B보다 작다(4.80 vs 4.68GiB로 비슷).
- **기존 3종 유지, 기본값 변경 없음.** `qwen3-4b-q4`가 여전히 기본이다 — 이미 그 id로 저장해 둔
  사용자가 있고, 기본을 바꾸면 새 설치가 커뮤니티 미러를 받게 된다. 바꾸고 싶으면
  `git/types.rs`의 `llm_model` 기본값 한 줄이다.

### 6.5 확인된 것 (열린 질문 해소)

- **사고 블록 누출 없음** — 7개 모델 × 4과제 28회 전부에서 `<think>`·`<|channel>`·`<|turn>`이
  `message.content`에 섞이지 않았다. `enable_thinking:false`가 실제로 동작한다는 실측이다.
- **Gemma 4 minja 렌더 완주** — E4B·12B 모두 기동·생성 정상. `--jinja` 불필요.
- **Gemma 4 E-변형 KV 경로** — E4B가 GPU 전체 오프로드(2,985→7,291MB)로 정상 동작.
- **8GB급 VRAM** — 12B는 4슬롯일 때 10,705MB를 써서 12GB 카드에서도 빠듯하다. `-np 1`로 9,328MB.

---

## 7. 부수 수정 — `-np 1`

`chat.rs`의 주석은 "**서버도 `-np 1`이라** 동시 요청은 컨텍스트 메모리를 두 배로 먹는다"고
단언하는데, **`server.rs`는 `-np`를 넘기지 않았다.** `arg.cpp`가 SERVER 예제에 `n_parallel = -1`
(auto)를 주므로 실제로는 **슬롯 4개**가 뜨고(`n_slots = 4, n_ctx_slot = 8192` — 기동 로그 실측)
쓰지도 않는 슬롯 3개가 KV 캐시를 차지했다.

`-np 1`을 넘기도록 고쳤다. 실측: Gemma 4 12B VRAM 10,705→9,328MB · 주간 요약 85→73초.
작은 모델은 속도 차가 없다(4B 2507 12.8→12.4초) — VRAM만 준다.

---

## 8. 프롬프트를 산문에서 템플릿으로 — 그리고 12B 복귀

§6 에서 "대부분의 모델이 `## 한 줄 요약`·`## 날짜별` 을 빠뜨린다"를 모델 탓으로 보지 않고
**프롬프트 탓**으로 가정해 고쳤다. `buildMessages()` 의 형식 제시를 산문
("형식: A / B / C")에서 **빈 템플릿**으로 바꾸고, 불릿에 `(100자 이내)` 를 덧붙였다.
불릿 개수 규칙("정확히 3개")은 **건드리지 않았다** — 00-INDEX §12.1 에 사용자 결정으로 적혀 있고,
거기 적힌 완화 조건("억지 문장이 보이면")은 여기서 관측한 문제(커밋 많은 날 과압축)와 방향이 다르다.

### 8.1 형식 준수 — 객관 지표 (주간 요약, 머리글 3개 중 몇 개 / 규격 외 `##` 제목 수)

| 모델 | 산문 (1회) | 템플릿 (3회) | 템플릿+100자 (2회) |
|---|---|---|---|
| Qwen3 1.7B Q8 | 2/3 · 0 | 2/3 · 0 | 2/3 · 0 (여전히 잘림) |
| Qwen3 4B Q4 | **1/3** · 0 | 3/3 · 0 | 3/3 · 0 |
| Qwen3 4B 2507 | 3/3 · 0 | 2/3 · 0 (**3회 모두 잘림**) | 3/3 · 0 · 잘림 0 |
| Gemma 4 E4B QAT | **1/3 · 여분 6** | 3/3 · 0 | 3/3 · 0 |
| Qwen3 8B Q4 | 3/3 · 0 | 3/3 · 0 (불릿 위반 3일) | 3/3 · 0 · 불릿 위반 0 |
| Gemma 4 12B QAT | 2/3 · 여분 2 | 3/3 · 0 | 3/3 · 0 · 불릿 위반 0 |

- **템플릿이 머리글 문제를 해결한다** — 규격 외 제목이 전 모델 0으로 떨어졌다(E4B 6개 → 0).
- **`(100자 이내)` 는 그 부작용을 막는다.** 템플릿만 주면 2507 이 불릿 하나를 380자까지 늘려
  5일 중 3일에서 잘렸다(상한을 3000 으로 올려도 여전히 잘렸다 — 상한 문제가 아니라 장황함 문제).
  길이를 못 박자 755·764 토큰으로 정상 종료하고 8B 의 불릿 개수 위반도 같이 사라졌다.
- 1.7B 는 어느 판에서도 못 지킨다(날짜 3~4개만 쓰고 잘린다). 산문판에서도 잘렸으니 회귀는 아니다.

### 8.2 블라인드 재채점 — 8B vs 12B (3회 × 심사원 3명 = 9표)

| 프롬프트 | 모델 | 형식 | 근거 | 한국어 | 총점 |
|---|---|---|---|---|---|
| 산문 | Qwen3 8B | 4.83 | 3.83 | 4.00 | **12.67** |
| 산문 | Gemma 4 12B | 2.33 | 4.39 | 4.83 | 11.56 |
| 템플릿 | Qwen3 8B | 3.56 | 3.11 | 3.44 | 10.11 |
| 템플릿 | Gemma 4 12B | 4.22 | 4.06 | 4.67 | **12.94** |
| **템플릿+100자** | Qwen3 8B | 3.89 | 2.44 | 2.89 | 9.22 |
| **템플릿+100자** | Gemma 4 12B | **5.00** | **4.33** | **4.78** | **14.11** |

세 회차 모두 심사원 전원이 출력 6개를 **정확히 모델별 3:3 으로** 갈라냈다 — 두 모델은 구분되는
차이를 낸다. 최종 판에서 12B 가 4.9점 앞선다.

**결정적 이유는 문체가 아니라 장부의 정확성이다.** 8B 는 실존 해시를 엉뚱한 커밋 설명에 매다는
오귀속을 출력마다 4~6건 낸다(예: `e2e 테스트 커버리지 확장 — d3477a1` 인데 d3477a1 은 품질 배치
커밋, `복사·인코딩·git log 창 — 377c6e5, 82fd68c, 0577035` 인데 그 셋은 전부 e2e 커밋). 심사원 1의
표현으로 "덜 다듬어진 요약이 아니라 **틀린 장부**"다. 12B 는 인접 커밋을 한 줄에 합친 경계 사례
1~2건뿐이다.

**속도 질문에 대한 답**: 심사원 3명에게 "한쪽이 4배 느리다면(13초 vs 50초) 예약 실행 전제에서
느린 쪽을 쓸 값어치가 있는가"를 물었고(어느 쪽이 느린지는 알려 주지 않았다) **3명 모두 "더 나은
쪽이 느린 쪽이면 지불할 만하다"**고 답했다. 그 더 나은 쪽이 12B 다. 그래서 12B 를 카탈로그에
되돌리고 note 에 `배치/예약 요약용 — 가장 정확, 느림(주간 ~50초)` 을 적었다.

### 8.3 기본 모델(4B) 검증은 **결론이 안 났다**

프롬프트 변경이 기본 경로를 해치지 않는지 보려고 4B 를 산문 3회 vs 템플릿 3회로 블라인드
비교했는데, **심사원 3명이 찾은 3:3 경계가 프롬프트 경계와 달랐다.** 그들이 가른 축은 "커밋 해시를
인용하는가"였고, 실제 배치는 산문·템플릿이 양쪽에 섞여 있었다 — 즉 **4B 는 같은 프롬프트에서도
어떤 회차는 해시를 인용하고 어떤 회차는 통째로 빼먹는다.** 회차 간 변동이 프롬프트 차이를
압도한다(총점 산문 8.78 vs 템플릿 7.22, 축별로는 서로 엇갈린다). 표본 결함도 하나 있다 —
템플릿 3개 중 하나가 길이 제한 없는 중간 판이었다.

여기서 나온 더 중요한 사실: **기본 모델 4B 는 프롬프트와 무관하게 주간 요약에 약하다**(7~9/15).
12B 의 14.11 과는 급이 다르다. 주간 리포트를 쓸 만하게 받으려면 모델을 올려야 한다.

---

## 9. 리포트 전용 모델 (`llm_report_model`)

§8 에서 "예약 요약은 12B, 대화는 작은 모델" 이 옳다는 결론이 났는데 설정이 전역 하나라 표현할 수
없었다. 필드 하나를 더해 갈랐다.

- `Settings.llm_report_model: Option<String>` — null/빈값이면 `llm_model` 을 쓴다(기본 null).
- `ChatReq.model_id` 로 **요청마다** 넘긴다. `ensure_server(.., want_model)` →
  `resolve_model(.., want)` 가 그 값을 우선한다. 60(요약)·67(리포트 채팅)만 채우고
  61(번역)·설정 테스트는 비운다 — 즉 "리포트만 다른 모델" 이 정확히 표현된다.
- `llmReadyReason(status, settings, modelId?)` 에 세 번째 인자를 더했다. **이걸 빼면**
  리포트가 12B 를 쓰는데 준비 판정이 기본 모델을 보므로 "준비됨" 이라 떠 있는 버튼이 요청 순간
  `모델이 없습니다` 로 죽는다.
- 설정 › AI › 고급에 선택 하나(`작업 리포트 전용 모델`, 기본 "위에서 고른 모델 사용").

**실기 확인**(dev 앱, 같은 세션에서 연속 3회): `llmModel=qwen3-4b-q4` ·
`llmReportModel=gemma4-12b-qat-q4` 로 두고 —

| 요청 | `modelId` | 띄운 모델 파일 | 경과 |
|---|---|---|---|
| 기본(번역·채팅 경로) | 없음 | `Qwen3-4B-Q4_K_M.gguf` | 5.0s |
| 리포트 | `gemma4-12b-qat-q4` | `gemma-4-12b-it-qat-q4_0.gguf` | 15.4s |
| 기본(되돌아오는지) | 없음 | `Qwen3-4B-Q4_K_M.gguf` | 9.3s |

**대가는 그대로 보인다** — 서버는 한 번에 한 모델만 물고 있어서(`ensure_server` 재사용 판정이
`s.model == model_id`) 번갈아 쓰면 매번 죽였다 다시 띄운다. 위 표의 15.4s·9.3s 가 그 전환 비용이다.
그래서 기본값을 null 로 두었다 — 켜는 사람은 "요약은 예약으로 돌리고 그때만 느려도 된다" 를
아는 사람이다.

---

## 10. 남은 것

- **예약 실행이 없다.** `모두 생성` 은 사용자가 누르는 배치다(`ReportView.tsx`). 12B 를 예약
  요약에 쓰려면 스케줄러가 먼저 필요하다. 지금은 설정에서 12B 를 고르고 `모두 생성` 을 누르는
  수동 경로만 있다.
- **기능별 모델은 §9 로 해결됐지만 전환 비용은 남는다**(실측 9~15초). 두 모델을 동시에 물고
  있게 하려면 세션을 모델별로 가져야 한다 — `AppState.llm` 이 단일 세션인 전제를 바꾸는 일이라
  안 했다(모델 하나가 수 GB를 mmap 한다는 §3.3 의 이유가 여전히 유효하다).
- **한 번에 한 요청이다**(`llm_inflight`). 12B 가 도는 ~50초 × 프로젝트 수 동안 번역·채팅이
  `Busy` 로 밀린다(`translate.ts` 가 3초 간격 최대 10분 재시도). 예약을 조용한 시각에 두거나
  큐가 필요하다.
- **4B 의 해시 인용이 회차마다 들쭉날쭉하다** — §8.3. 기본 모델을 바꿀지, 프롬프트를 더 손댈지는
  미결이다. 재측정하려면 **같은 판**의 표본만 3개씩 쓸 것(이번의 표본 결함).
- **8B 의 근거 점수가 길이 제한에서 떨어진다**(3.83 → 2.44). 100자에 맞추려 두 커밋을 한 줄에
  뭉치면서 오귀속이 늘어난 것으로 보인다 — 12B 는 같은 제한에서 그러지 않는다. 8B 를 계속
  쓰는 사용자에게는 산문판이 나았다. 프롬프트를 모델별로 가를 가치가 있는지는 미검증.
- **1.7B 는 주간 요약을 못 한다**(날짜 3~4개만 쓰고 잘림, 두 프롬프트 모두). 저사양 티어는
  일간 요약·번역까지만 기대하는 게 맞다.
- **12B 의 13 t/s 원인 미확정.** `-np 1` 로도 안 풀렸다. 가중치 6.5GiB + KV 가 12GB 카드의
  가용 VRAM(다른 앱이 ~3GB 점유)을 넘겨 공유 메모리로 흘러넘치는 것으로 보이나 확증하지 않았다.
- **`promptTokens`/`completionTokens` 는 항상 0 이다.** 앱이 `stream_options:{include_usage:true}`
  를 보내지 않아 스트리밍 응답에 usage 가 없다. 프론트에서 쓰는 곳이 없어 방치했다.
